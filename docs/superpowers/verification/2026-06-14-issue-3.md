# Issue 3: UOS ARM64 Text Capture Verification

## Status

BLOCKED

## Git Reference

Implementation commit: f965225 (`fix: parse real UOS X11 input events`)

## Automated Tests

```
 node --test tests/linux-x11-selection-watcher.test.js tests/package-build.test.js
 Result: 27 passed, 0 failed

 npm test
 Result: 193 passed, 0 failed
```

## Evidence Files

- Environment collector: `scripts/collect-uos-capture-evidence.sh`
- Structure test: `tests/uos-evidence-script.test.js`
- Manual matrix: `docs/superpowers/verification/issue-3/uos-arm64-x11-manual-matrix.md`

## Blocker

UOS ARM64 X11 hardware unavailable for end-to-end validation.

## Required Hardware Evidence

The following must be captured on UOS ARM64 X11 hardware:

1. Run: `./scripts/collect-uos-capture-evidence.sh`
2. Build: `npm run build:linux:arm64`
3. Install: `sudo apt-get install -y ./dist/*.deb`
4. Execute `docs/superpowers/verification/issue-3/uos-arm64-x11-manual-matrix.md`
5. Record results in verification document

## Platform Scope

- Deepin: REJECTED (scope is UOS only)
- Missing XDG_SESSION_TYPE: REJECTED (must be explicit 'x11')
- Required commands: xinput, xdotool, xclip

## Real XI2 Parser Evidence

- The watcher parses the real multi-line `xinput test-xi2 --root` shape: `EVENT type`, `detail:`, and `root:` on separate lines.
- Stdout chunks retain incomplete trailing lines until the next chunk.
- Legacy single-line fixtures remain supported for focused unit tests.
- A real-format drag fixture produces one capture at the release coordinates.
- The deb packaging test requires `xinput`, `xdotool`, and `xclip`.

## Changed Files

### Core Implementation
- `src/main/platform-info.js` - UOS detection, X11 session check, correct command list
- `src/main/linux-selected-text-reader.js` - X11 PRIMARY selection reading via xclip
- `src/main/linux-x11-selection-watcher.js` - xinput-based gesture detection
- `src/main/text-capture.js` - Platform routing to appropriate backend

### Integration
- `src/main/index.js` - IPC handlers for capture status
- `src/main/tray.js` - Conditional tray menu based on capture backend availability

### Settings UI
- `src/renderer/settings/index.html` - Capture status display section
- `src/renderer/settings/script.js` - Status polling and UI updates
- `src/renderer/settings/style.css` - Status indicator styling
- `src/preload/settings-preload.js` - IPC exposure for settings renderer

### Build Configuration
- `package.json` - Linux ARM64 deb target, X11 tool dependencies

### Documentation
- `README.md` - Updated with Linux support information

### Tests
- `tests/platform-info.test.js` - Platform detection tests
- `tests/linux-selected-text-reader.test.js` - X11 selection reader tests
- `tests/linux-x11-selection-watcher.test.js` - Gesture watcher tests
- `tests/package-build.test.js` - ARM64 deb dependency contract
- `tests/text-capture-platform-route.test.js` - Platform routing tests
- `tests/text-capture-ignored-gesture.test.js` - Deterministic pending-capture regression test
- `tests/uos-evidence-script.test.js` - Evidence script structure test

### Scripts
- `scripts/collect-uos-capture-evidence.sh` - Environment evidence collector

### Verification
- `docs/superpowers/verification/2026-06-14-issue-3.md` - This verification document
- `docs/superpowers/verification/issue-3/uos-arm64-x11-manual-matrix.md` - Required hardware matrix

## Known Limitations

1. **Wayland not supported** - Implementation only supports X11
2. **UOS only (NOT Deepin)** - Specifically for UOS on ARM64
3. **Explicit X11 session required** - XDG_SESSION_TYPE must be exactly 'x11'
4. **Dependencies required** - xinput, xdotool, xclip must be installed

## Next Steps

1. Transfer to UOS ARM64 X11 machine
2. Run evidence collector script
3. Execute `docs/superpowers/verification/issue-3/uos-arm64-x11-manual-matrix.md`
4. Update status to PASS or document specific failures
